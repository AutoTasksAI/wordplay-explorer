.env.local
node_modules
dist
# src/convex/_generated is intentionally tracked so Cloudflare Pages builds
# without needing an authenticated Convex CLI step. Regenerate with:
#   npx convex dev --once
# src/convex/_generated
